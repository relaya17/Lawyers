import React from 'react'
import { Navigate } from 'react-router-dom'

/** תוכן המעבדה הועבר למקורות המשפט — תחת מבחנים (חוזים / עבודה). */
export const SimulatorPage: React.FC = () => <Navigate to="/legal-knowledge" replace />
